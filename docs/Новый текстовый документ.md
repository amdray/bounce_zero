Проанализировал представленный файл `main.c`. Ниже приведен список выявленных нарушений, характерных для разработки под PSP и портирования логики с других платформ (судя по комментариям о Java).

---

# нарушения

**тип нарушения:** костыль / отход от нативного API
**место нарушения:** файл `main.c` функция `util_open_file`.
**описание нарушения:** Ручной перебор префиксов путей (`ms0:/`, `./`) и манипуляции со слешами. В среде PSP SDK (pspdev) стандартная библиотека `libc` и драйверы устройств (I/O) корректно обрабатывают относительные пути от корня папки приложения. Кроме того, жесткое использование `ms0:/` (Memory Stick) приведет к поломке на PSP Go, где используется внутренняя память `ef0:/`.
**рекомендации к исправлению:** Рекомендуется инициализировать рабочую директорию один раз при запуске через `argv[0]` или использовать относительные пути без ручной склейки строк. Для работы с файлами лучше полагаться на стандартные функции, предварительно убедившись, что `cwd` (current working directory) установлена верно.

---

**тип нарушения:** архитектурный просчет
**место нарушения:** файл `main.c` функция `main` (игровой цикл).
**описание нарушения:** Громоздкая проверка состояний игры в условии `if` (8 различных состояний). Это делает основной цикл трудночитаемым и усложняет добавление новых экранов. Логика переключения состояний размыта между `main` и `game_update`.
**рекомендации к исправлению:** Реализовать классический паттерн "State Machine". Каждое состояние (меню, игра, заставка) должно иметь свои функции `update` и `render`, вызываемые через указатели или через четко структурированный `switch`.

---

**тип нарушения:** велосипед / тонкая логика
**место нарушения:** файл `main.c` функция `main` (обработка ввода).
**описание нарушения:** Ручная буферизация нажатий кнопок (`g_game.buffered_jump = 1` и т.д.) прямо в основном цикле. Это попытка имитировать событийную модель Java в процедурном C. Если физический тик (30 мс) не успеет выполниться (например, при `steps == 0`), буферы не будут сброшены, что может привести к ложным или повторным срабатываниям в следующем кадре.
**рекомендации к исправлению:** Перенести логику обработки "событий" (нажато/отпущено) внутрь модуля `input.c`. Использовать битовые маски для хранения текущего состояния кнопок и состояния кнопок на предыдущем кадре, чтобы вычислять факт нажатия (edge detection) централизованно.

---

**тип нарушения:** тонкая логика (потенциальный баг)
**место нарушения:** файл `main.c` функция `main` (блок `STATE_GAME`).
**описание нарушения:** Сброс буферов ввода (`g_game.buffered_jump = 0`) происходит внутри цикла `while (physics_time_acc_ms >= PHYSICS_DT_MS)`. Если за один кадр рендеринга накопилось время на два тика физики, ввод будет обработан в первом тике и обнулен для второго. Если же время для тика еще не накопилось (`steps == 0`), ввод останется "активным" на следующий кадр рендеринга, хотя фактически кнопка могла быть уже отпущена.
**рекомендации к исправлению:** Сброс накопленного ввода (consumable input) должен быть строго детерминирован. Либо ввод должен накапливаться до тех пор, пока не выполнится хотя бы один шаг физики, либо логика должна разделять "состояние кнопки сейчас" и "факт нажатия с прошлого кадра".

---

**тип нарушения:** архитектурный просчет / костыль
**место нарушения:** файл `main.c` функция `main` (завершение работы).
**описание нарушения:** Использование `sceKernelTerminateDeleteThread(callback_thid)` для завершения потока колбэков. В PSPSDK это считается "грубым" методом. Поток колбэков обычно находится в состоянии `sceKernelSleepThreadCB()`, и его принудительное удаление может оставить системные ресурсы в неопределенном состоянии.
**рекомендации к исправлению:** Вместо принудительного удаления потока, правильнее позволить ему завершиться самому (если это предусмотрено логикой) или просто полагаться на `sceKernelExitGame`, который корректно завершает все потоки процесса и возвращает управление в меню консоли (VSH).

---

**тип нарушения:** ошибка в коде (безопасность)
**место нарушения:** файл `main.c` функция `util_open_file`.
**описание нарушения:** Использование `snprintf` с фиксированным размером буфера `512` без проверки длины входящего аргумента `path`. Несмотря на защиту от переполнения в `snprintf`, итоговый путь может быть обрезан, что приведет к попытке открыть несуществующий файл, при этом функция не сообщит о критической длине пути.
**рекомендации к исправлению:** Добавить проверку входной строки на максимальную длину `MAX_PATH` и возвращать `NULL` немедленно, если путь превышает допустимый размер буфера.


---

# нарушения

**тип нарушения:** тонкая логика / архитектурная хрупкость
**место нарушения:** файл `game.c` функция `deactivate_ring_pair`.
**описание нарушения:** "Магические числа" и жесткая привязка логики к ID тайлов (13, 14, 17, 18, 21 и т.д.). Если в будущем вы измените атлас (tileset) или порядок тайлов в редакторе, механика сбора колец и чекпоинтов сломается без ошибок компиляции. Логика "соседства" (изменение `x, y+1`) также жестко зашита в код.
**рекомендации к исправлению:** Рекомендуется использовать метаданные тайлов, которые уже упоминаются в коде (`tile_meta_db`). Вместо проверки конкретных ID, следует проверять свойства тайла (например, `is_collectible`) и хранить в метаданных ссылку на ID "неактивного" состояния этого же тайла.

---

**тип нарушения:** архитектурный просчет (performance bottleneck)
**место нарушения:** файл `game.c` функция `game_render`.
**описание нарушения:** Избыточное переключение состояний графического конвейера (State Switching). Внутри одной функции рендеринга многократно вызываются `graphics_begin_textured` и `graphics_begin_plain`. На PSP каждое такое переключение между режимом текстур и примитивов (GU_TEXTURE_2D on/off) — это дорогая операция для видеочипа, сбрасывающая кэши и пайплайн.
**рекомендации к исправлению:** Сгруппируйте отрисовку. Сначала отрендерите всё, что требует текстур (уровень, игрока, иконки HUD), а затем всё, что требует только заливки цветом (рамки, полоски бонуса, текст). Это позволит свести количество переключений режима к минимуму (в идеале — одно переключение за кадр).

---

**тип нарушения:** нерациональное использование ресурсов (VRAM)
**место нарушения:** файл `game.c` функция `game_init`.
**описание нарушения:** Загрузка сплэш-экранов (Nokia, Bounce) напрямую в VRAM (`png_load_texture_vram`). Видеопамять PSP крайне ограничена (всего 2 МБ). Хранить там статичные изображения, которые показываются один раз при запуске, — расточительство. Это может привести к тому, что для основных игровых текстур или двойной буферизации не хватит места в быстрой памяти.
**рекомендации к исправлению:** Загружайте одноразовые заставки в обычную оперативную память (RAM) и выгружайте их сразу после перехода в меню. В VRAM должны находиться только те ресурсы, которые используются в каждом кадре игрового процесса (тайлы, спрайты игрока).

---

**тип нарушения:** велосипед
**место нарушения:** файл `game.c` функция `game_render` (отрисовка большого мяча).
**описание нарушения:** Ручное создание "составного" спрайта 2x2 с вычислением трансформаций (`PNG_TRANSFORM_FLIP_X/Y`) в цикле для каждой четверти большого мяча. Это типичный подход для платформ, где нет аппаратного масштабирования. PSP поддерживает аппаратную трансформацию и масштабирование текстур "из коробки".
**рекомендации к исправлению:** Вместо ручной сборки из четырех кусков, используйте аппаратные возможности PSP GU для отрисовки одного спрайта с коэффициентом масштабирования (scaling) или просто добавьте в атлас готовую текстуру большого мяча.

---

**тип нарушения:** тонкая логика / дублирование
**место нарушения:** файл `game.c` функция `game_update` (обработка ввода).
**описание нарушения:** Смешивание логики событий (`buffered_left_pressed`) и состояний (`input_held`) в одном условии. Это создает ситуацию "двойного управления", где физика может среагировать дважды на одно нажатие или проигнорировать отпускание кнопки. Кроме того, это дублирует часть работы, которая уже должна быть инкапсулирована в `input.c`.
**рекомендации к исправлению:** Игровой цикл должен полагаться на чистые состояния "кнопка нажата в данном тике физики". Логика определения "была ли нажата кнопка с момента последнего тика" должна быть полностью скрыта внутри модуля `input`, возвращая `game.c` только конечный результат (флаг действия).

---

**тип нарушения:** архитектурный просчет (God Object)
**место нарушения:** файл `game.c` функции `game_update` и `game_render`.
**описание нарушения:** Файл `game.c` берет на себя ответственность за логику сплэш-экранов (`STATE_SPLASH_NOKIA`). Это не относится к игровой механике (physics/player/level). При расширении игры (например, добавлении экрана титров или настроек) этот файл превратится в нечитаемый список `switch-case` для всех состояний приложения.
**рекомендации к исправлению:** Вынесите логику заставок в отдельный модуль или передайте её в `menu.c`. Модуль `game.c` должен отвечать только за состояние `STATE_GAME` (непосредственно игровой процесс).

---

# нарушения

**тип нарушения:** критический архитектурный просчет / костыль
**место нарушения:** файл `graphics.c` функции `graphics_draw_number` и `graphics_draw_button_x`.
**описание нарушения:** Попиксельная отрисовка шрифта и иконки. Для каждого закрашенного пикселя вызывается `graphics_draw_rect`, которая внутри инициирует полноценный вызов отрисовки `sceGuDrawArray`. Для одного числа из 5 цифр это может породить тысячи вызовов отрисовки за кадр. На реальной PSP это приведет к катастрофическому падению FPS, так как GPU будет тратить всё время на обработку команд, а не на саму отрисовку.
**рекомендации к исправлению:** Перенесите шрифт 24-го размера и иконку кнопки в общую текстуру (атлас). Отрисовывайте символ или иконку целиком одним вызовом отрисовки как спрайт, а не по точкам.

---

**тип нарушения:** велосипед / неэффективное использование ресурсов
**место нарушения:** файл `graphics.c` функция `graphics_draw_text`.
**описание нарушения:** Текст отрисовывается посимвольно через `sceGuDrawArray` прямо в цикле. При этом в этом же файле уже реализована система батчинга (`graphics_batch_sprite`), но она здесь не используется. Каждый символ — это отдельный Draw Call. Если на экране много текста, это сильно нагружает графический конвейер.
**рекомендации к исправлению:** Интегрируйте отрисовку текста в существующую систему батчинга. Вместо немедленного вызова `sceGuDrawArray` для каждого глифа, добавляйте вершины символов в массив батча и сбрасывайте его (flush) только при смене текстурной страницы атласа или в конце строки.

---

**тип нарушения:** тонкая логика / избыточное копирование памяти
**место нарушения:** файл `graphics.c` функции `graphics_flush_batch` и `graphics_batch_sprite`.
**описание нарушения:** Двойная буферизация вершин в RAM. Сначала данные записываются в статический массив `s_batch.vertices`, а затем копируются через `memcpy` в память, выделенную через `sceGuGetMemory`. Это создает лишнюю нагрузку на CPU и шину памяти, которая является узким местом на PSP.
**рекомендации к исправлению:** Измените логику работы батча. Вместо накопления в промежуточном массиве, запрашивайте порцию памяти у GU в начале формирования батча (или при его переполнении) и записывайте данные вершин напрямую в адресное пространство, возвращаемое `sceGuGetMemory`.

---

**тип нарушения:** отход от нативного API / потенциальный баг
**место нарушения:** файл `graphics.c` функция `graphics_init`.
**описание нарушения:** Директивное указание адресов буферов кадра как `(void*)0` и `(void*)framebuffer_size`. Хотя это работает, это "магическое" распределение VRAM. Если в будущем вы добавите Z-буфер или дополнительные текстуры в VRAM, возникнет конфликт адресов, который трудно отладить. Также не учитывается, что адреса для `sceGuDrawBuffer` должны быть выровнены и часто указываются как относительные адреса внутри VRAM.
**рекомендации к исправлению:** Используйте более формализованный менеджер VRAM (например, через утилиту `vramalloc` или аналоги), чтобы динамически распределять адреса для Draw Buffer, Display Buffer и текстур. Это предотвратит наложение данных при усложнении графики.

---

**тип нарушения:** архитектурный просчет (State Machine)
**место нарушения:** файл `graphics.c` функции `graphics_begin_textured` и `graphics_begin_plain`.
**описание нарушения:** Принудительный сброс батча (`graphics_flush_batch`) внутри функций смены режима. Если игровой код часто переключается между текстурами и примитивами (как было замечено в `game.c`), это сводит на нет всю эффективность батчинга, превращая его в серию одиночных вызовов.
**рекомендации к исправлению:** Оптимизируйте вызовы в игровом цикле так, чтобы сначала отрисовывались все текстурированные объекты, а затем все примитивы. В самих функциях `graphics.c` добавьте проверку: если нужный режим уже активен, не делайте лишних системных вызовов `sceGuEnable/Disable`.

# нарушения

**тип нарушения:** архитектурный просчет / велосипед
**место нарушения:** файл `png.c` функция `getStaticVramBuffer`.
**описание нарушения:** Реализован простейший «стековый» или «bump» аллокатор видеопамяти, который только увеличивает смещение (`staticVramOffset`). В коде полностью отсутствует механизм освобождения или повторного использования участков VRAM.
**рекомендации к исправлению:** Рекомендуется реализовать более полноценный менеджер VRAM (например, на основе списков свободных блоков) или использовать готовые библиотеки для аллокации (например, `vramalloc`). В текущем виде приложение неизбежно исчерпает видеопамять при любой перезагрузке уровня или частой смене текстур.

---

**тип нарушения:** ошибка в коде / утечка памяти
**место нарушения:** файл `png.c` функция `png_free_texture`.
**описание нарушения:** При удалении текстуры, находящейся в VRAM, функция освобождает только саму управляющую структуру `texture_t`, но никак не помечает область в видеопамяти как свободную (из-за отсутствия менеджера памяти, описанного выше). Это делает невозможным циклическую загрузку ресурсов.
**рекомендации к исправлению:** Необходимо связать систему освобождения памяти с аллокатором VRAM. Если используется простейший указатель, то освобождение возможно только в порядке, обратном загрузке (LIFO), либо требуется полная очистка VRAM при смене игровых состояний.

---

**тип нарушения:** тонкая логика / костыль
**место нарушения:** файл `png.c` функция `png_load_texture_vram` (копирование данных).
**описание нарушения:** Ручное попиксельное копирование данных из `image_data` в `dest` через вложенные циклы. Это крайне медленная операция для процессора PSP (Allegrex). Кроме того, если текстура загружается в RAM, используется `memset` и циклы вместо эффективного копирования блоков.
**рекомендации к исправлению:** Для копирования целых строк изображения следует использовать `memcpy`. В идеале, для загрузки данных в VRAM стоит использовать механизм DMA, что позволит процессору заниматься другими задачами во время передачи текстуры.

---

**тип нарушения:** архитектурный просчет (performance killer)
**место нарушения:** файл `png.c` функция `png_draw_sprite_uv4`.
**описание нарушения:** Функция принудительно вызывает `graphics_flush_batch()` перед каждой отрисовкой. Так как эта функция используется через `png_draw_sprite_transform` для рендеринга больших объектов (например, большого мяча, состоящего из 4 частей), батчинг полностью ломается. Вместо того чтобы отрисовать 4 спрайта одним вызовом, видеочип получает 4 отдельных команды отрисовки с переключением контекста между ними.
**рекомендации к исправлению:** Следует расширить систему батчинга так, чтобы она поддерживала произвольные UV-координаты для вершин в рамках одного типа примитива (`GU_SPRITES` или `GU_TRIANGLES`), либо вынести `flush` на уровень выше, вызывая его только при реальной необходимости смены текстуры или режима блендинга.

---

**тип нарушения:** отход от нативного API / тонкая логика
**место нарушения:** файл `png.c` переменная `staticVramOffset`.
**описание нарушения:** Начальное смещение для аллокатора VRAM жестко задано как двойной размер фреймбуфера. Это не учитывает возможного использования Z-буфера (глубины), который может потребоваться даже в 2D (например, для сложной сортировки слоев), а также не учитывает особенности выравнивания адресов для различных режимов дисплея.
**рекомендации к исправлению:** Начальный адрес свободной VRAM должен вычисляться динамически на основе параметров, переданных в `sceGuDrawBuffer` и `sceGuDispBuffer`, учитывая фактическое наличие и размер буфера глубины.

---

# нарушения

**тип нарушения:** архитектурный просчет / тонкая логика
**место нарушения:** файл `input.c` функция `input_update`.
**описание нарушения:** Использование `sceCtrlReadLatch`. Эта функция при каждом вызове возвращает изменения состояния кнопок и **сбрасывает** внутренний счетчик защелки (latch) в системном драйвере. Поскольку в вашем `main.c` функция `input_update` вызывается на каждом кадре рендеринга (часто), а логика физики работает по своему таймеру, возникают ситуации, когда нажатие кнопки считывается в `input_update`, но "стирается" из состояния до того, как физический тик успеет вызвать `input_pressed`. Это классическая проблема потери событий ввода при рассинхронизации циклов.
**рекомендации к исправлению:** Рекомендуется полностью отказаться от `SceCtrlLatch`. Вместо этого используйте только `sceCtrlReadBufferPositive`. Для определения нажатий и отпусканий храните маску кнопок предыдущего кадра. Однако для того, чтобы не терять быстрые нажатия между тиками физики, модуль должен не просто хранить состояние, а "накапливать" (через логическое ИЛИ) все нажатия, произошедшие между вызовами функции сброса событий.

---

**тип нарушения:** отход от нативного API / "велосипед" (в контексте UX)
**место нарушения:** файл `input.c` функция `input_init`.
**описание нарушения:** Принудительная установка `PSP_CTRL_MODE_DIGITAL`. На PSP это отключает опрос аналогового стика. Несмотря на то, что оригинальный Bounce — игра под кнопочные телефоны, для PSP-сообщества считается "хорошим тоном" дублировать действия крестовины на аналоговый стик. В текущем режиме стик полностью мертв, и его нельзя использовать даже для навигации в меню.
**рекомендации к исправлению:** Переключитесь на `PSP_CTRL_MODE_ANALOG`. В функциях проверки движения добавьте проверку отклонения стика (например, если значение по оси X больше 128 + порог чувствительности). Это сделает управление более современным и удобным для консоли.

---

**тип нарушения:** тонкая логика
**место нарушения:** файл `input.c` функции `input_pressed`, `input_held`, `input_released`.
**описание нарушения:** Модуль является пассивным и не имеет понятия о "потреблении" ввода. Если игроку нужно гарантированно отловить нажатие кнопки прыжка ровно один раз для одного действия, текущая реализация заставляет его либо надеяться на удачу, либо городить внешние буферы (что мы и видим в `main.c`).
**рекомендации к исправлению:** Реализуйте внутри модуля систему событий. Разделите понятия "состояние кнопки в данный момент" и "событие нажатия, произошедшее с момента последней очистки". Добавьте функцию для очистки накопленных событий, которую будет вызывать игровой цикл в конце обработки физики.

---

**тип нарушения:** ошибка в коде (потенциальная)
**место нарушения:** файл `input.c` функция `input_update`.
**описание нарушения:** В случае ошибки чтения `sceCtrlReadBufferPositive` (возврат <= 0), переменная `s_buttons` обнуляется. Это может вызвать ложное событие "отпускания" всех зажатых кнопок (release) прямо посреди игрового процесса, если драйвер на мгновение вернет ошибку.
**рекомендации к исправлению:** В случае неудачного чтения буфера лучше сохранять состояние предыдущего успешного кадра или игнорировать обновление в этой итерации, чтобы избежать резких скачков в логике управления.

---

Модуль `input.c` — это фундамент, и его исправление позволит вам выкинуть огромный кусок кода с `buffered_` переменными из `main.c`.


---

# нарушения

**тип нарушения:** архитектурный просчет (performance bottleneck)
**место нарушения:** файл `level.c` функция `render_moving_spikes_tile`.
**описание нарушения:** Внутри функции рендеринга тайла вызывается `level_find_moving_object_at`, которая выполняет цикл по всем движущимся объектам уровня. Поскольку этот вызов происходит для каждого видимого тайла шипов в каждом кадре, сложность отрисовки растет пропорционально количеству объектов и площади их покрытия. На PSP это приведет к заметным "просадкам" кадров на уровнях с большим количеством ловушек.
**рекомендации к исправлению:** Рекомендуется изменить подход: вместо поиска объекта для тайла, нужно итерироваться по списку активных движущихся объектов один раз за кадр и отрисовывать их поверх карты, либо кэшировать указатель на объект прямо в сетке тайлов при загрузке уровня.

---

**тип нарушения:** критический архитектурный просчет / отход от нативного API
**место нарушения:** файл `level.c` функция `level_render_visible_area`.
**описание нарушения:** Массовое переключение состояний графического конвейера (`graphics_begin_plain` / `graphics_begin_textured`) внутри вложенного цикла отрисовки тайлов. В худшем случае (шахматный порядок вода/земля) конвейер PSP будет переинициализироваться сотни раз за один проход рендера. Это лишает смысла систему батчинга спрайтов, так как батч будет принудительно сбрасываться (flush) почти на каждом тайле.
**рекомендации к исправлению:** Необходимо разделить рендеринг на слои. Сначала отрисовывать все фоновые тайлы (plain rects) одним проходом, затем все текстурированные тайлы (спрайты) вторым проходом. Это позволит минимизировать переключения состояний до 1-2 раз за кадр.

---

**тип нарушения:** ошибка в коде / уязвимость (buffer overflow)
**место нарушения:** файл `level.c` функция `level_load_from_memory`.
**описание нарушения:** Парсер данных уровня последовательно инкрементирует `offset++` без проверки границ буфера `dataSize` перед каждым чтением. Если файл уровня поврежден или имеет неверный формат (например, количество объектов больше, чем данных в файле), приложение попытается прочитать память за пределами выделенного буфера, что приведет к `Crash` (Exception) на реальной консоли.
**рекомендации к исправлению:** Добавить макрос или вспомогательную функцию для безопасного чтения байта, которая проверяет текущий `offset` относительно `dataSize` и немедленно прекращает парсинг с возвратом ошибки, если данные закончились преждевременно.

---

**тип нарушения:** велосипед / тонкая логика
**место нарушения:** файл `level.c` функция `render_exit_tile`.
**описание нарушения:** Реализация ручного "клиппинга" (обрезания) спрайта двери через пересчет UV-координат и создание новых структур `clipped_r`. PSP поддерживает аппаратный клиппинг через `sceGuScissor`. Текущая реализация сложна, подвержена ошибкам округления и создает лишнюю нагрузку на CPU для вычислений, которые GPU может сделать бесплатно.
**рекомендации к исправлению:** Вместо манипуляций с UV-координатами следует установить область `sceGuScissor`, соответствующую дверному проему (24x24), отрисовать спрайты двери со смещением и вернуть `scissor` в исходное полноэкранное состояние.

---

**тип нарушения:** архитектурный просчет (статический лимит)
**место нарушения:** файл `level.c` функция `hoop_fg_push`.
**описание нарушения:** Использование фиксированного массива `s_hoop_fg` с лимитом `RING_FG_QUEUE_MAX`. Если на одном экране окажется больше колец, чем размер очереди (например, в плотном бонусном уровне), часть колец просто перестанет отрисовываться (точнее, их передний план). Это создает визуальный баг "шар поверх кольца".
**рекомендации к исправлению:** Поскольку кольца всегда привязаны к тайлам, эффективнее добавить в структуру `TileMeta` или в саму карту флаг необходимости отрисовки переднего плана. Тогда вторым проходом рендеринга можно будет просто отрисовать нужные части колец, не используя промежуточную очередь.

---

**тип нарушения:** тонкая логика / костыль
**место нарушения:** файл `level.c` функция `render_hoop_tile`.
**описание нарушения:** Жестко зашитые (hardcoded) трансформации для разных типов колец прямо в коде функции. Это делает невозможным использование колец под другими углами (например, диагональных) без дописывания кода в этом модуле. Логика "если ID от 13 до 28" дублирует информацию, которая должна быть в метаданных.
**рекомендации к исправлению:** Перенесите параметры трансформации фона и переднего плана в таблицу `TileMeta`. Функция рендеринга должна просто брать готовое значение `transform` из таблицы, не зная, какой именно ID тайла она сейчас рисует.

---

# нарушения

**тип нарушения:** ошибка в коде / критическое узкое место (performance killer)
**место нарушения:** файл `physics.c` функция `squareCollide`.
**описание нарушения:** Использование `logger_printf` внутри вложенных циклов попиксельной коллизии. Даже если логирование закрыто условием `if (g_probe_active)`, сама проверка и подготовка аргументов для функции в горячем цикле физики (который может вызываться десятки раз за кадр из-за саб-степпинга) создают колоссальную нагрузку на CPU. На реальном железе PSP это приведет к резкому падению производительности при каждом столкновении.
**рекомендации к исправлению:** Полностью удалите логирование из функций коллизий. Для отладки лучше использовать визуализацию векторов или состояний непосредственно на экране во время рендеринга.

---

**тип нарушения:** тонкая логика / костыль
**место нарушения:** файл `physics.c` глобальные переменные `g_probe_active`, `g_probe_yhit_active`.
**описание нарушения:** Использование глобальных флагов для передачи контекста отладки («зондирования») внутрь глубоко вложенных функций коллизий. Это создает скрытые зависимости и побочные эффекты, делая код крайне сложным для отладки и модификации. Функция начинает вести себя по-разному в зависимости от состояния глобальной переменной, установленной где-то в другом месте.
**рекомендации к исправлению:** Если функции коллизии нужен контекст для отладки или специфического поведения, передавайте его как аргумент функции или создайте отдельную функцию для «зондирования» (probes), которая инкапсулирует эту логику.

---

**тип нарушения:** архитектурный просчет / "магические числа"
**место нарушения:** файл `physics.c` функции `testTile`, `thinCollide`, `edgeCollide`.
**описание нарушения:** Жесткая привязка логики к конкретным ID тайлов (например, `tileID >= 13 && tileID <= 24`, `tileID == 10`). Это прямое наследие ограничений J2ME, которое в C-коде превращается в трудноподдерживаемый «спагетти-код». Любое изменение в атласе тайлов потребует переписывания всей физики.
**рекомендации к исправлению:** Используйте систему флагов или свойств тайлов в метаданных (`TileMeta`). Физика должна проверять свойства (например, `is_lethal`, `is_bouncy`, `is_hoop`), а не конкретные номера картинок в атласе.

---

**тип нарушения:** тонкая логика (риск зависания)
**место нарушения:** файл `physics.c` функция `enlarge_ball`.
**описание нарушения:** Цикл `while (!found_free_space)` с инкрементом `offset++` для поиска свободного места при увеличении мяча. В случае, если игрок находится в полностью закрытом пространстве или сложной геометрии, этот цикл может выполняться слишком долго или привести к непредсказуемому перемещению мяча на другой конец карты.
**рекомендации к исправлению:** Добавьте жесткое ограничение на количество итераций цикла (safety limit). Реализуйте алгоритм выталкивания (depenetration) на основе векторов нормалей коллизий, а не перебором всех направлений с увеличивающимся шагом.

---

**тип нарушения:** велосипед / неэффективная реализация
**место нарушения:** файл `physics.c` функция `player_update`.
**описание нарушения:** Ручной пошаговый цикл (sub-stepping) для осей X и Y отдельно с делением скорости на `MOVEMENT_STEP_DIVISOR`. Хотя это обеспечивает совместимость с оригиналом, такой подход игнорирует аппаратные возможности PSP и современные методы обработки коллизий (например, через Sweep-тесты). Многократный вызов тяжелой функции `collisionDetection` внутри циклов `xSteps` и `ySteps` избыточен.
**рекомендации к исправлению:** Оптимизируйте проверку коллизий, сначала вычисляя ограничивающий прямоугольник (Bounding Box) всего движения за тик, и проверяйте только те тайлы, которые в него попадают. Рассмотрите возможность использования целочисленной математики с фиксированной запятой (fixed-point) для более точного контроля скорости без потери производительности.

---

**тип нарушения:** тонкая логика (хрупкость структуры)
**место нарушения:** файл `physics.c` функция `squareCollide`.
**описание нарушения:** Использование имен переменных `b1`, `n`, `b2`, `i1`, взятых из декомпилированного Java-кода. В контексте C это делает код практически нечитаемым для человека. Понять логику «обрезки» (clipping) мяча по границам тайла без обращения к оригиналу невозможно.
**рекомендации к исправлению:** Проведите рефакторинг с использованием осмысленных имен переменных (например, `min_x_clip`, `max_y_boundary`). Добавьте комментарии, объясняющие математическую суть попиксельного наложения масок.

---

# нарушения

**тип нарушения:** костыль / неэффективный I/O
**место нарушения:** файл `sound.c` функция `parse_ott`.
**описание нарушения:** Чтение файла происходит побайтово в цикле с использованием `getc`. На PSP работа с картой памяти (Memory Stick) через стандартные функции `libc` по одному байту работает крайне медленно из-за накладных расходов на каждый вызов и отсутствие буферизации на уровне драйвера.
**рекомендации к исправлению:** Рекомендуется определять размер файла через `fseek`/`ftell` и считывать весь объем данных одним вызовом `fread` в заранее выделенный буфер.

---

**тип нарушения:** тонкая логика / ошибка в коде
**место нарушения:** файл `sound.c` функции `parse_ott` и `get_bits`.
**описание нарушения:** Использование "магического числа" `16738` в качестве жесткого ограничения размера буфера. Это значение взято из старых исходников `ringtonetools` и не имеет смысла в современной системе. Если OTT файл окажется чуть больше, он будет обрезан, а если меньше — парсер может начать читать неинициализированные данные, несмотря на `memset`.
**рекомендации к исправлению:** Используйте динамическое выделение памяти на основе реального размера файла или хотя бы константу, соответствующую размеру выделенного массива. Передавайте размер буфера в `get_bits` как аргумент для проверки границ.

---

**тип нарушения:** велосипед / архитектурный просчет
**место нарушения:** файл `sound.c` функция `ott_audio_callback`.
**описание нарушения:** Ручное смешивание (микширование) трех аудио-потоков в одном callback-канале. PSP имеет аппаратную поддержку нескольких аудио-каналов (до 8 штук), которые микшируются системным софт-процессором или железом. Создание собственного микшера внутри callback — это лишняя трата ресурсов CPU Allegrex, которые могли бы пойти на игровую логику или графику.
**рекомендации к исправлению:** Рекомендуется использовать `pspaudiolib` для открытия трех независимых каналов (по одному на каждый тип звука). Это упростит код: каждый канал будет иметь свой callback только для одного осциллятора, а системная библиотека сама выполнит смешивание.

---

**тип нарушения:** архитектурный просчет (performance killer)
**место нарушения:** файл `sound.c` функции `ott_audio_callback` и `generate_player_sample`.
**описание нарушения:** Использование вычислений с плавающей точкой (`float`) внутри критического цикла аудио-обработки. Процессор PSP (MIPS 4KE) имеет FPU, но операции с плавающей точкой всё равно медленнее целочисленных. Особенно критично использование `1.0f / (float)PSP_SR` и постоянные преобразования типов внутри цикла, который выполняется 44100 раз в секунду для каждого канала.
**рекомендации к исправлению:** Полностью переведите внутренний цикл генерации звука на целочисленную арифметику с фиксированной запятой (fixed-point). Предвычисляйте все константы времени (`sample_length`) заранее.

---

**тип нарушения:** тонкая логика (race condition)
**место нарушения:** файл `sound.c` функция `ott_player_start`.
**описание нарушения:** Использование `sceKernelCpuSuspendIntr()` для обеспечения атомарности запуска звука. Это слишком "тяжелый" метод для синхронизации с аудио-потоком. Приостановка всех прерываний системы ради установки нескольких переменных в плеере может негативно сказаться на других системных процессах (например, на работе Wi-Fi или I/O).
**рекомендации к исправлению:** Для синхронизации аудио-коллбэка и основного потока лучше использовать флаг "занятости" или простые атомарные операции. Поскольку данные только пишутся в основном потоке и только читаются в аудио-потоке, достаточно правильно выставить флаг `is_playing` в самом конце после инициализации всех параметров ноты.

---

**тип нарушения:** архитектурный просчет
**место нарушения:** файл `sound.c` функция `sound_init`.
**описание нарушения:** Жесткая привязка трех конкретных звуковых эффектов к глобальным переменным внутри модуля. Это мешает расширяемости (например, если вы захотите добавить фоновую музыку или четвертый звук).
**рекомендации к исправлению:** Создайте массив структур `ott_player_t` или систему динамической регистрации звуков. Это позволит загружать любое количество эффектов без изменения кода функций `sound_init` и `ott_audio_callback`.

---

# нарушения

**тип нарушения:** архитектурный просчет / тонкая логика
**место нарушения:** файл `save.c` функции `save_init` и `save_store_data`.
**описание нарушения:** Использование прямого дампа структуры (`fwrite(&g_save_data, sizeof(SaveData)...)`) в файл. Это создает проблему «хрупкости» сохранений: если в будущем вы добавите хотя бы одно поле в середину или начало структуры `SaveData` (в `types.h`), старые файлы сохранений станут невалидными или, что хуже, будут прочитаны со смещением данных, что приведет к непредсказуемому поведению. В коде также отсутствует поле `version`.
**рекомендации к исправлению:** Добавьте в начало структуры поле версии (например, `uint32_t version`). При загрузке проверяйте версию и реализуйте логику миграции данных. Также рекомендуется сохранять поля по отдельности или использовать выравнивание, чтобы избежать проблем с паддингом (padding) компилятора при изменении структуры.

---

**тип нарушения:** отход от нативного API / костыль
**место нарушения:** файл `save.c` макрос `SAVE_FILE_PATH`.
**описание нарушения:** Путь `"bounce_save.dat"` без указания устройства или директории. На PSP это приведет к попытке записи в корень текущего рабочего устройства. Это нарушает правила организации данных в среде PSP: пользовательские данные должны либо лежать в папке с игрой (`./`), либо использовать системный путь `ms0:/PSP/SAVEGAME/`. Кроме того, жесткий путь не учитывает PSP Go (внутренняя память `ef0:/`).
**рекомендации к исправлению:** Используйте относительный путь (например, `./save.dat`) или динамически формируйте путь, используя базовую директорию приложения, полученную из `argv[0]`. Для полноценного соответствия стандартам PSP следует использовать системный диалог `sceUtilitySavedata`, который создает официальные слоты сохранений с иконками в меню консоли.

---

**тип нарушения:** тонкая логика (риск потери данных)
**место нарушения:** файл `save.c` функция `save_store_data`.
**описание нарушения:** Небезопасная процедура записи. Функция открывает существующий файл в режиме `"wb"`, что немедленно уничтожает старое содержимое. Если в процессе записи произойдет сбой (разрядится аккумулятор или пользователь выключит консоль), файл останется пустым или поврежденным, и весь прогресс игрока будет потерян.
**рекомендации к исправлению:** Используйте стратегию «атомного сохранения». Записывайте данные во временный файл (например, `save.tmp`), и только после успешного закрытия этого файла используйте системную функцию для переименования или удаления старого файла и подстановки нового.

---

**тип нарушения:** архитектурный просчет (избыточность операций)
**место нарушения:** файл `save.c` функция `save_update_records`.
**описание нарушения:** Сохранение в файл происходит при каждом обновлении рекорда. Если в конце уровня игрок одновременно побил рекорд по очкам и открыл новый уровень, функция может быть вызвана дважды, инициируя медленные операции записи на карту памяти.
**рекомендации к исправлению:** Разделите логику обновления данных в памяти и логику физической записи на диск. Вызывайте `save_store_data` только в ключевые моменты (выход в меню, завершение уровня) или добавьте небольшую задержку (таймер), чтобы объединить несколько изменений в одну операцию записи.

---

# нарушения

**тип нарушения:** архитектурный просчет
**место нарушения:** файл `menu.c` функции `instructions_render`, `high_score_render`, `game_over_render`, `level_complete_render`.
**описание нарушения:** Многократное дублирование кода отрисовки графического интерфейса. Отрисовка "белой панели" (модального окна) размером 240x128 с вычислением координат центра повторяется в четырех разных функциях почти без изменений. Это затрудняет изменение дизайна всей игры (например, если вы захотите изменить размер или цвет всех панелей сразу).
**рекомендации к исправлению:** Рекомендуется вынести логику отрисовки стандартного диалогового окна в отдельную функцию (например, в `graphics.c` или вспомогательный UI-модуль). Функция должна принимать параметры заголовка, основного текста или callback для отрисовки содержимого внутри панели.

---

**тип нарушения:** архитектурный просчет / производительность
**место нарушения:** файл `menu.c` функция `menu_render`.
**описание нарушения:** Локальное объявление и инициализация массива структур `items[5]` прямо внутри функции рендеринга. Этот массив (содержащий строки и координаты) заново создается на стеке и заполняется данными при каждом вызове функции (обычно 60 раз в секунду). Это неэффективное использование ресурсов процессора и стека.
**рекомендации к исправлению:** Вынесите массив определений пунктов меню в статическую область памяти (`static const`) или инициализируйте его один раз при запуске модуля. Функция рендеринга должна только считывать данные и переключать цвета.

---

**тип нарушения:** велосипед / производительность
**место нарушения:** файл `menu.c` функция `draw_wrapped_text`.
**описание нарушения:** Алгоритм автоматического переноса строк реализован крайне неэффективно для PSP. Для нахождения точки разрыва строки функция посимвольно наращивает строку и вызывает `graphics_measure_text` (которая сама является циклом) на каждой итерации. Сложность вычислений растет квадратично относительно длины текста.
**рекомендации к исправлению:** Оптимизируйте алгоритм, измеряя ширину текста по словам, а не по символам. В идеале, для статичных текстов (как инструкции) расчет переносов должен выполняться один раз при загрузке страницы, а не каждый кадр рендеринга.

---

**тип нарушения:** тонкая логика / архитектурная хрупкость
**место нарушения:** файл `menu.c` функция `menu_update`.
**описание нарушения:** Использование магических чисел (0, 1, 2, 3, 4) в блоке `if-else` для обработки выбора игрока. При этом в начале файла уже объявлено перечисление `MenuItem`, которое призвано заменить эти числа. В текущем виде код легко сломать: если вы измените порядок пунктов в `enum`, но забудете поменять индексы в `menu_update`, игра начнет запускать не те функции.
**рекомендации к исправлению:** Замените все числовые проверки на константы из перечисления `MenuItem`. Это гарантирует, что логика всегда будет соответствовать порядку элементов в меню.

---

**тип нарушения:** архитектурный просчет
**место нарушения:** файл `menu.c` функции `menu_update`, `level_select_update`, `level_complete_update`.
**описание нарушения:** Модуль меню берет на себя ответственность за глубокую инициализацию игрового процесса. Вызов `player_init`, `level_load_by_number`, ручной сброс очков, жизней и камеры — все это "раскидано" по функциям меню. Это приводит к дублированию кода (например, логика запуска уровня в `menu_update` и `level_select_update` почти идентична) и усложняет отладку физики.
**рекомендации к исправлению:** Инкапсулируйте логику старта игры в модуле `game.c`. Меню должно вызывать одну высокоуровневую функцию (например, `game_start_level(int number)`), которая сама позаботится о сбросе всех необходимых структур, загрузке карты и настройке камеры.

---

**тип нарушения:** тонкая логика / риск переполнения
**место нарушения:** файл `menu.c` функция `draw_wrapped_text`.
**описание нарушения:** Использование фиксированного буфера `temp_line[256]` без проверки на длину входного слова. Если в тексте попадется очень длинная строка без пробелов или специфический символ, это может привести к выходу за границы массива или некорректному завершению строки.
**рекомендации к исправлению:** Добавьте проверку границ при заполнении `temp_line` или используйте указатели для работы с частями исходной строки без копирования в промежуточный буфер.

---

**тип нарушения:** тонкая логика
**место нарушения:** файл `menu.c` функция `level_select_render`.
**описание нарушения:** Жестко зашитая сетка 5x2 (и отдельная обработка 11-го уровня). Это типичный "костыль", который не позволит вам легко добавить 12-й или 15-й уровень в будущем без переписывания математики отрисовки.
**рекомендации к исправлению:** Реализуйте отрисовку сетки уровней через универсальный цикл, который рассчитывает количество строк и столбцов на основе константы `MAX_LEVEL`.

---

# нарушения

**тип нарушения:** архитектурный просчет / костыль
**место нарушения:** файл `tile_table.c` массив `TILE_DB`.
**описание нарушения:** Использование жестко заданного статического массива метаданных для всех игровых тайлов. Все свойства (индексы в атласе, трансформации, типы рендеринга) "зашиты" в код. Это делает невозможным изменение визуальной составляющей или добавление новых типов объектов без полной перекомпиляции проекта. Кроме того, наличие пустых ячеек (case 11, 12) указывает на хрупкую структуру, завязанную на порядковые индексы.
**рекомендации к исправлению:** Рекомендуется вынести описание метаданных тайлов во внешний конфигурационный файл (например, бинарный или текстовый), который будет загружаться динамически при инициализации уровня или игры.

---

**тип нарушения:** отход от нативного API / костыль
**место нарушения:** файл `main.c` функция `util_open_file`.
**описание нарушения:** Ручное переключение префиксов путей (`ms0:/`, `./`). Жесткое использование `ms0:/` (Memory Stick) приведет к тому, что приложение не найдет ресурсы на консолях PSP Go при их установке во внутреннюю память (`ef0:/`). Также в PSPSDK стандартная библиотека `libc` корректно обрабатывает относительные пути от корня папки приложения.
**рекомендации к исправлению:** Следует использовать относительные пути без указания диска. Установку корректной рабочей директории (CWD) стоит производить один раз при запуске через анализ `argv[0]`.

---

**тип нарушения:** тонкая логика / костыль
**место нарушения:** файл `main.c` основной цикл `while`.
**описание нарушения:** Ручное накопление событий ввода (`g_game.buffered_jump = 1` и другие) прямо в главном цикле. Это попытка компенсировать потерю нажатий между кадрами рендеринга (60 FPS) и тиками физики (30 FPS). Если шаг физики не будет выполнен в текущем кадре (из-за задержки), а буферы будут перезаписаны или сброшены некорректно, действие игрока будет потеряно или сработает дважды.
**рекомендации к исправлению:** Перенести логику фиксации событий нажатия (edge detection) внутрь модуля `input.c`, используя систему накопления флагов, которые будут сбрасываться только после их обработки игровым циклом.

---

**тип нарушения:** ошибка в коде / отход от нативного API
**место нарушения:** файл `main.c` завершение работы приложения.
**описание нарушения:** Использование `sceKernelTerminateDeleteThread(callback_thid)` для завершения системного потока колбэков. В среде PSP принудительное удаление потока (Terminate) считается плохой практикой, так как может оставить ядро системы в нестабильном состоянии или привести к утечке дескрипторов ресурсов.
**рекомендации к исправлению:** Вместо принудительного убийства потока следует позволить ему завершиться самостоятельно (например, выставив флаг выхода) или просто вызывать `sceKernelExitGame`, который автоматически и корректно завершает все потоки приложения.

---

**тип нарушения:** архитектурный просчет
**место нарушения:** файл `main.c` функция `main` (блок инициализации).
**описание нарушения:** Последовательная инициализация всех систем без проверки возвращаемых значений. Если `sound_init` или `graphics_init` вернут ошибку (например, из-за нехватки памяти VRAM или отсутствия файлов), приложение продолжит выполнение и упадет с критической ошибкой (Exception) позже, что затруднит поиск причины.
**рекомендации к исправлению:** Добавить проверку кодов возврата для всех функций инициализации систем. В случае критической ошибки следует выводить сообщение пользователю и корректно завершать работу.

---

# нарушения

**тип нарушения:** архитектурный просчет / потенциальная утечка памяти
**место нарушения:** файл `local.c` функция `initialize_string_cache`.
**описание нарушения:** Выделение памяти через `malloc(file_size)` происходит без проверки результата на `NULL` перед использованием в `fread`. На PSP с её ограниченной оперативной памятью (24 МБ доступно приложениям) попытка загрузить большой языковой файл или фрагментация памяти могут привести к краху системы при попытке записи в нулевой указатель.
**рекомендации к исправлению:** Всегда проверяйте результат `malloc` и `realloc`. В случае ошибки выделения памяти функция должна прекращать работу и возвращать код ошибки, а не пытаться читать данные.

---

**тип нарушения:** велосипед / низкая производительность
**место нарушения:** файл `local.c` функция `str_replace`.
**описание нарушения:** Реализация собственной функции замены подстроки. В языке C и стандартных библиотеках PSP SDK нет прямой функции `replace`, но ручное манипулирование `memcpy` и `strcpy` для каждого параметра в цикле (в `local_get_text_with_params`) создает лишнюю нагрузку. Кроме того, функция ограничена фиксированными буферами `MAX_STRING_LENGTH`.
**рекомендации к исправлению:** Вместо полной замены подстроки в буфере рекомендуется использовать функцию отрисовки, которая умеет подставлять аргументы «на лету» (аналог `printf`), либо формировать конечную строку один раз при помощи `snprintf` с форматной строкой, полученной из ресурсов.

---

**тип нарушения:** тонкая логика / риск переполнения
**место нарушения:** файл `local.c` функция `local_get_text_with_params`.
**описание нарушения:** Использование `static char` буферов для возврата результата. Это делает функцию не реентерабельной. Если вызвать её дважды в одном выражении (например, в аргументах `printf` или при отрисовке двух разных строк в одном кадре), второй вызов перезапишет данные первого. Также размер `MAX_STRING_LENGTH` (1024) может быть недостаточен для длинных инструкций с несколькими подставленными параметрами.
**рекомендации к исправлению:** Либо передавайте буфер для результата из вызывающей функции (caller-owned buffer), либо используйте динамическое выделение памяти, если время жизни строки должно быть долгим.

---

**тип нарушения:** костыль / отход от нативного API
**место нарушения:** файл `local.c` функция `detect_phone_language`.
**описание нарушения:** Ручное сопоставление кодов языков PSP с внутренними строковыми идентификаторами (`ru-RU`, `de`, `xx`). Это «зашивает» поддержку языков в код.
**рекомендации к исправлению:** Рекомендуется использовать системные коды `PSP_SYSTEMPARAM_LANGUAGE_*` напрямую как часть имени файла (например, `lang/lang.11` для русского) или создать таблицу соответствия во внешнем файле, чтобы добавление нового перевода не требовало изменения кода модуля.

---

**тип нарушения:** тонкая логика (безопасность)
**место нарушения:** файл `local.c` функция `initialize_string_cache` (парсинг смещений).
**описание нарушения:** Чтение двухбайтовых смещений и длин строк через побитовые сдвиги `(buffer[offset] << 8) | buffer[offset + 1]`. Хотя это решает проблему порядка байт (endianness), в коде отсутствуют проверки на то, что вычисленное смещение или длина строки не выходят за реальные границы считанного буфера. Поврежденный lang-файл приведет к чтению произвольной памяти.
**рекомендации к исправлению:** Введите строгую проверку: `string_offset + length` всегда должен быть меньше или равен `file_size`.

---

**тип нарушения:** костыль / дублирование
**место нарушения:** файл `local.c` функции `local_text_select_level` и `local_text_settings`.
**описание нарушения:** Хардкод строк «Выбор уровня» и «Настройки» прямо в C-файле. Это нарушает саму концепцию интернационализации (i18n), ради которой создан этот модуль. Часть строк берется из файлов, а часть — из условий `strncmp` в коде.
**рекомендации к исправлению:** Перенесите все строковые ресурсы в файлы `lang.*`. Если строка отсутствует в оригинальном наборе Bounce, добавьте её в конец файла, расширив `MAX_STRING_ID`.

---

# нарушения

**тип нарушения:** архитектурный просчет (God Object)
**место нарушения:** файл `types.h`, структура `Game`.
**описание нарушения:** Структура `Game` является классическим «антипаттерном» God Object. Она объединяет в себе абсолютно разнородные данные: системное состояние приложения (`state`), состояние пользовательского интерфейса (`menu_selection`, `instruction_part`), физическое состояние игрока (`player`), глобальную игровую статистику (`score`, `numLives`) и даже технические буферы ввода. Это создает сверхвысокую связность (coupling) — любой модуль, которому нужно просто изменить счет, получает доступ к управлению текстурами сплэш-экранов и навигации в меню.
**рекомендации к исправлению:** Рекомендуется разделить эту структуру на несколько независимых сущностей. Выделите `AppState` для системных флагов, `PlayerStats` для прогресса и `UIState` для навигации. Передавайте в функции только те части данных, которые им действительно необходимы для работы.

---

**тип нарушения:** велосипед (эмуляция фиксированной запятой)
**место нарушения:** файл `types.h`, структура `Player`.
**описание нарушения:** Использование обычных `int` для координат (`xPos`, `yPos`) и скоростей с логикой «x0.1 пикселя/кадр» является попыткой вручную реализовать арифметику с фиксированной запятой (fixed-point). Для процессора MIPS (PSP) стандартом является использование битовых сдвигов (например, формат 24.8). Текущий подход с делением на `MOVEMENT_STEP_DIVISOR` (10) работает значительно медленнее побитовых операций и вносит погрешности при расчетах векторов.
**рекомендации к исправлению:** Перейдите на стандартную арифметику с фиксированной запятой, где дробная часть занимает фиксированное количество бит (например, 8 бит). Это позволит использовать быстрые побитовые сдвиги вместо деления и обеспечит более высокую точность физических расчетов.

---

**тип нарушения:** архитектурный просчет (глобальное состояние)
**место нарушения:** файл `types.h`, объявление `extern Game g_game;`.
**описание нарушения:** Использование глобальной переменной для доступа ко всем данным игры провоцирует написание кода с побочными эффектами (side effects). Это крайне затрудняет отладку и тестирование, так как любая функция в любом модуле может непредсказуемо изменить состояние игрока или сбросить выбор в меню, не заявляя об этом в своих аргументах.
**рекомендации к исправлению:** Избавьтесь от `extern` объявления в общем заголовочном файле. Передавайте указатели на конкретные структуры данных в функции в качестве аргументов. Это сделает зависимости между модулями явными и прозрачными.

---

**тип нарушения:** костыль (буферизация ввода в игровой логике)
**место нарушения:** файл `types.h`, поля `buffered_...` в структуре `Game`.
**описание нарушения:** Наличие технических полей для хранения нажатых кнопок внутри главной структуры игрового мира — это «грязный» способ решения проблемы рассинхронизации частоты опроса кнопок и физического цикла. Это нарушает принцип разделения ответственности: игровая логика не должна знать о том, как именно опрашивается «железо».
**рекомендации к исправлению:** Логика накопления событий нажатия должна быть полностью скрыта внутри модуля `input.c`. Модуль ввода должен предоставлять функции, которые возвращают факт возникновения события с момента последней обработки, избавляя структуру `Game` от необходимости хранить эти промежуточные состояния.

---

**тип нарушения:** тонкая логика (жесткие ограничения контента)
**место нарушения:** файл `types.h`, макрос `MAX_LEVEL 11`.
**описание нарушения:** Жестко заданное максимальное количество уровней на уровне препроцессора. Это ограничивает расширяемость игры: добавление 12-го уровня потребует перекомпиляции всех модулей, включающих этот заголовок. Также это принуждает UI-код (меню) полагаться на константу вместо реального наличия файлов на диске.
**рекомендации к исправлению:** Определяйте количество уровней динамически при запуске приложения (например, путем сканирования директории с уровнями) или вынесите это значение в загружаемый конфигурационный файл.

---

**тип нарушения:** архитектурный просчет (отсутствие версионирования)
**место нарушения:** файл `types.h`, структура `SaveData`.
**описание нарушения:** В структуре данных для сохранения отсутствует поле версии. При портировании или обновлении игры любое изменение состава этой структуры (добавление новых полей или изменение их порядка) приведет к тому, что старые файлы сохранений на карте памяти будут прочитаны со смещением. Это вызовет порчу данных или крах приложения.
**рекомендации к исправлению:** Добавьте поле `uint32_t version` в самое начало структуры `SaveData`. При загрузке файла всегда проверяйте это значение, чтобы обеспечить корректную миграцию данных или их безопасный сброс.

---

# нарушения

**тип нарушения:** архитектурный просчет / смешивание уровней абстракции
**место нарушения:** файл `graphics.h` функции `graphics_draw_number`, `graphics_draw_button_x`, `utf8_decode_to_codepoint`.
**описание нарушения:** Заголовочный файл базовой графической системы содержит функции для отрисовки специфических игровых элементов (кнопки контроллера PSP, числа через конкретный шрифт) и утилиту декодирования текста. Это нарушает принцип единственной ответственности: графическое ядро не должно знать о деталях игрового интерфейса или кодировках строк.
**рекомендации к исправлению:** Вынесите логику декодирования строк в отдельный вспомогательный модуль. Функции отрисовки элементов интерфейса (кнопки, специфические шрифты) перенесите в специализированный модуль UI, оставив в графическом ядре только универсальные примитивы.

**тип нарушения:** костыль (принудительное управление состоянием)
**место нарушения:** файл `graphics.h` функции `graphics_begin_plain`, `graphics_begin_textured`.
**описание нарушения:** Перекладывание ответственности за управление состоянием текстурирования на вызывающий код через «Протокол использования». Это создает хрупкую архитектуру, где пропуск одного системного вызова в игровом цикле приводит к визуальным артефактам, которые сложно отследить в больших проектах.
**рекомендации к исправлению:** Реализуйте автоматическое управление состоянием внутри функций отрисовки. Каждая функция должна самостоятельно проверять текущий статус текстурирования и изменять его только при необходимости, используя внутреннее кэширование состояния в модуле графики.

**тип нарушения:** архитектурный просчет (экспонирование деталей VRAM)
**место нарушения:** файл `graphics.h` макросы `VRAM_BUFFER_WIDTH`, `VRAM_BUFFER_HEIGHT`.
**описание нарушения:** Определение параметров физической раскладки видеопамяти в публичном заголовке. Это делает систему зависимой от конкретной аппаратной реализации и затрудняет изменение параметров хранения (например, смену Pixel Storage Mode) без перекомпиляции всех зависимых модулей.
**рекомендации к исправлению:** Инкапсулируйте расчеты адресов и размеров буферов VRAM внутри реализации модуля. Публичный заголовок должен оперировать только логическими размерами экрана, необходимыми для игровой логики.

**тип нарушения:** тонкая логика (несоответствие типов данных)
**место нарушения:** файл `graphics.h` функции `graphics_draw_text`, `graphics_draw_rect`.
**описание нарушения:** Использование `float` для экранных координат в сочетании с `int` для параметров шрифтов без явной стратегии округления. На PSP работа с плавающей запятой в 2D часто приводит к субпиксельным артефактам («дрожанию» или щелям), если вызывающий код не выполняет приведение типов вручную.
**рекомендации к исправлению:** Используйте целочисленные типы для 2D-координат там, где важна попиксельная точность, либо внедрите использование фиксированной запятой (fixed-point). При сохранении `float` добавьте внутреннее приведение к целым числам непосредственно перед передачей координат в графический конвейер.

---

# нарушения

**тип нарушения:** архитектурный просчет
**место нарушения:** файл `png.h` структура `texture_t`.
**описание нарушения:** Модуль, названный в честь формата файлов (PNG), содержит базовое определение текстуры для всей игры. Это создает жесткую привязку: если в будущем возникнет необходимость использовать нативные для PSP форматы (например, GIM) или сжатые текстуры (PVR/ETC), придется либо хранить их в структуре "png_t", либо переписывать все вызовы отрисовки во всем проекте.
**рекомендации к исправлению:** Рекомендуется переименовать модуль и структуры в более абстрактные (например, `texture.h` и `Texture`), отделив логику загрузки конкретного формата (PNG) от логики хранения и использования графических ресурсов.

---

**тип нарушения:** тонкая логика / ошибка в коде
**место нарушения:** файл `png.h` функции `png_draw_sprite` и `png_draw_sprite_uv4`.
**описание нарушения:** Несогласованность типов данных для экранных координат. В функции `png_draw_sprite` параметры `x, y` принимаются как `int`, а в `png_draw_sprite_uv4` — как `float`. Это вынуждает вызывающий код проводить лишние преобразования типов и может привести к визуальным артефактам (дрожанию пикселей или щелям в 1 пиксель) из-за разной точности округления координат при отрисовке одного и того же объекта разными методами.
**рекомендации к исправлению:** Необходимо привести все функции отрисовки к единому стандарту типов координат. Для 2D-игры на PSP оптимальным выбором является использование `int` для попиксельной точности или `float` с обязательным внутренним округлением перед передачей в графический конвейер.

---

**тип нарушения:** велосипед / архитектурный просчет
**место нарушения:** файл `png.h` структура `sprite_rect_t`.
**описание нарушения:** Использование нормализованных UV-координат (от 0.0 до 1.0) в формате `float` для 2D-спрайтов. Хотя это стандарт для современных графических API, видеочип PSP (GU) работает эффективнее с прямыми пиксельными координатами. Текущий подход требует выполнения операций умножения `float * int` при каждой отрисовке каждого спрайта, что создает ненужную нагрузку на CPU без практической пользы, так как размеры атласов фиксированы.
**рекомендации к исправлению:** Рекомендуется хранить границы спрайтов в пиксельных координатах (используя типы `int` или `short`). Это позволит передавать данные в GU напрямую, избегая математических расчетов в "горячем" цикле рендеринга.

---

**тип нарушения:** архитектурный просчет (размытие ответственности)
**место нарушения:** файл `png.h` перечисление `png_transform_t`.
**описание нарушения:** Логика трансформации спрайтов (повороты, зеркалирование) вынесена в модуль загрузки изображений. Трансформация — это свойство процесса отрисовки и графического конвейера, а не формата файла или способа хранения данных в памяти.
**рекомендации к исправлению:** Перенесите перечисление трансформаций в заголовочный файл графической системы (`graphics.h`). Это позволит использовать его в других модулях без необходимости подключать библиотеку загрузки PNG.


### 1. Пиксель-перфект integer scaling (Zoom)

**Сложность: Средняя**

Реализация масштабирования (например, 2x) потребует изменений в нескольких модулях, так как текущий код завязан на фиксированные размеры экрана PSP (480x272).

- Изменения в рендеринге: Вам придется модифицировать функции отрисовки в graphics.c и png.c. Поскольку вы используете GU_TRANSFORM_2D, координаты и размеры спрайтов в функциях png_draw_sprite и graphics_draw_rect нужно будет умножать на коэффициент зума.
- Логика камеры: В level.c функция level_render_visible_area рассчитывает количество видимых тайлов исходя из screenWidth и screenHeight. При включении зума видимая область уровня уменьшится, поэтому параметры startTile и endTile должны пересчитываться с учетом масштаба.
- Интерфейс (HUD): Масштабирование шрифтов в graphics_draw_text сложнее, так как они отрисовываются посимвольно из атласа. Вам потребуется масштабировать каждый прямоугольник (rect) символа при выводе.
- Рекомендация: Проще всего реализовать это через глобальный коэффициент масштаба, который будет применяться во всех функциях graphics_draw_* и png_draw_*.

### 2. Пересчет физики с 30 до 60 кадров

**Сложность: Высокая (из-за рисков нарушения аутентичности)**

Простое изменение констант может сломать «чувство» игры, так как физика Bounce сильно завязана на целочисленные вычисления и фиксированный шаг.

- Изменение тайминга: В main.c нужно изменить PHYSICS_DT_MS с 30 на 15 или 16.
- Масштабирование констант: Если физика будет вызываться в 2 раза чаще, вам теоретически нужно уменьшить вдвое все значения ускорения (HORZ_ACCELL, GRAVITY_ACCELL) и силы прыжка (JUMP_STRENGTH) в types.h.
- Проблема округления: Многие константы в types.h — нечетные или маленькие (например, NORMAL_GRAVITY_ACCELL 4, FRICTION_DECELL 4). Если после деления на 2 вы получите нецелое число, а код продолжит использовать int, мяч начнет двигаться иначе: он может не допрыгивать до платформ или иметь другую инерцию.
- MOVEMENT_STEP_DIVISOR: Физика в physics.c использует пошаговое перемещение, деля скорость на 10 (MOVEMENT_STEP_DIVISOR). Это ядро оригинальной механики. Переход на 60 FPS потребует глубокого тестирования того, как эти «микро-шаги» суммируются в коллизиях.
- Рекомендация: Вместо изменения констант физики, лучше оставить её работать на 30 Гц, но внедрить интерполяцию координат при рендеринге в game_render. Это даст визуальную плавность 60 FPS без риска сломать логику столкновений.

### Итог

- Зум: Добавить относительно легко, если централизованно внедрить множитель координат в graphics.c.
- 60 FPS Физика: Очень рискованно. Математика в physics.c слишком специфична. Лучше использовать графическую интерполяцию, сохраняя логический тик 30 мс.

---

Портирование приложения на **vitasdk** для PS Vita является задачей средней сложности. Основной объем работы связан с заменой системных API и графического конвейера, в то время как логика и физика на языке C переносятся практически без изменений.

## Технический анализ портирования

---

### 1. Графическая подсистема (Наибольшая сложность)

Графический чип PSP работает с фиксированным конвейером (`GU`), в то время как PS Vita использует программируемый конвейер (`GXM`).

- Отрисовка: Прямые вызовы sceGu... из graphics.c и png.c несовместимы с Vita. Портирование потребует либо написания шейдеров на языке CG, либо использования оберток, таких как libvita2d, которая предоставляет функции, аналогичные вашим (vita2d_draw_rectangle, vita2d_draw_texture).
- Разрешение: Экран Vita имеет разрешение 960x544, что ровно в 2 раза больше PSP (480x272). Это делает реализацию "пиксель-перфект зума" тривиальной, так как один пиксель PSP идеально ложится в сетку 2x2 или 4x4 пикселя Vita без артефактов интерполяции.

### 2. Ввод и системные API

- Константы: Имена кнопок и функций меняются (например, префикс PSP_CTRL_ меняется на SCE_CTRL_, а sceCtrlReadLatch заменяется на sceCtrlPeekBufferPositive).
- Потоки: Система колбэков выхода (Exit Callback) на Vita реализована иначе, чем на PSP, и требует использования системных событий sceAppMgr.

### 3. Звук и файловая система

- Аудио: API звука на Vita (SceAudioOut) отличается от pspaudiolib. Синтезатор OTT в sound.c останется прежним, но функцию заполнения буфера (callback) нужно будет адаптировать под формат Vita.
- Пути: Пути ms0:/ или ./ должны быть заменены на системные разделы Vita, такие как ux0:data/ВашеПриложение/.

### 4. Физика и логика (Низкая сложность)

- Модули physics.c, level.c, local.c и tile_table.c написаны на стандартном C и не используют специфичных для PSP вызовов. Они переносятся путем простой перекомпиляции под архитектуру ARM (vitasdk использует arm-vita-eabi-gcc).

---

### Сводная таблица изменений

| Модуль | Уровень сложности | Основная задача |
| --- | --- | --- |
| graphics.c | Высокий | Замена GU на GXM или vita2d |
| input.c | Низкий | Смена констант PSP_ на SCE_ |
| sound.c | Средний | Адаптация callback под SceAudioOut |
| physics.c | Нулевой | Только перекомпиляция |
| save.c | Низкий | Смена путей к файлам на ux0: |

---

Портирование вашего приложения на **Nintendo 3DS (devkitARM / libctru)** по сложности сопоставимо с портом на PS Vita, но имеет свои уникальные архитектурные нюансы, связанные с аппаратным обеспечением и двойным экраном.

---

# Анализ портирования на Nintendo 3DS

**Сложность: Средняя**

### 1. Графическая система (PICA200 и Citro2D)

3DS использует графический чип PICA200, который сильно отличается от PSP GU.

- API: Вместо pspgu вам придется использовать citro3d (низкоуровневый) или, что более логично для вашего проекта, citro2d. Последний по духу очень близок к вашему graphics.c: он также работает с батчингом спрайтов и атласами.
- Разрешение: Это главный «подводный камень». Разрешение верхнего экрана 3DS — 400x240 (против 480x272 у PSP). Ваш код в level.c и menu.c жестко завязан на ширину 480. При переносе часть игрового поля будет обрезана, либо потребуется пересчет логики видимой области (level_render_visible_area).
- Текстуры: 3DS требует, чтобы текстуры в памяти были «тайлированными» (tiled format). Вы не сможете просто скопировать массив пикселей из stb_image в VRAM, как вы делали в png.c. Потребуется использовать утилиты типа tex3ds или программную конвертацию.

### 2. Двухэкранная архитектура

Текущий код (`main.c` и `game_render`) рассчитан на один экран.

- Разделение ответственности: На 3DS хорошим тоном считается вынос HUD (счет, жизни) на нижний экран (320x240). Это потребует разделения функции game_render на две части и изменения структуры Game для управления состояниями обоих экранов.

### 3. Звук (NDSP)

- Аудио-движок: В sound.c используется callback-система PSP. На 3DS библиотека libctru использует сервис ndsp. Вам придется переписать систему подачи сэмплов: вместо прямого заполнения буфера в прерывании, нужно будет организовать циклическую очередь буферов (wave buffers). Логика синтеза OTT перенесется без изменений.

### 4. Ввод и управление

- HID: Модуль input.c потребует минимальных правок. sceCtrlReadBufferPositive заменяется на hidKeysDown() и hidKeysHeld().
- Аналоговое управление: В отличие от PSP, Circle Pad на 3DS очень надежен. Будет крайне просто добавить поддержку плавного движения мяча.

---

### Вердикт по модулям

| Модуль | Статус при портировании |
| --- | --- |
| physics.c | Полная совместимость. ARM11 процессор 3DS легко справится с попиксельными коллизиями. |
| level.c | Требует доработки. Необходим пересчет координат под экран 400x240. |
| graphics.c | Полная замена. Весь код на pspgu должен быть переписан на citro2d. |
| local.c | Полная совместимость. |
| save.c | Минимальные правки. Изменение путей к файлам на sdmc:/. |

---

### Итоговое сравнение сложности

Если сравнивать с PS Vita:

1. На Vita проще сделать «красиво» из-за огромного разрешения и мощного GPU.
2. На 3DS проще сохранить «ретро-дух» игры, но сложнее адаптировать интерфейс из-за более узкого экрана и необходимости работы с двумя дисплеями.

В целом, ваш C-код достаточно чист для того, чтобы опытный разработчик перенес его на 3DS за 1–2 недели.

---


Анализ всей совокупности файлов выявил несколько критических глобальных нарушений ответственности модулей. Эти проблемы не ограничиваются одним файлом, а пронизывают всю архитектуру приложения, создавая избыточную связанность (coupling).

---

# Глобальные нарушения

**тип нарушения:** God Object (Божественный объект)
**место нарушения:** файл `types.h` структура `Game`, файл `main.c` переменная `g_game`.
**описание нарушения:** Структура `Game` нарушает принцип разделения ответственности на фундаментальном уровне. Она объединяет системное состояние (текущий экран), игровую статистику (очки, жизни), физические данные игрока и технические буферы ввода. В результате почти каждый модуль в проекте (`physics`, `menu`, `level`, `save`) вынужден подключать `types.h` и работать с одной и той же глобальной переменной. Это делает систему крайне хрупкой: изменение в логике меню может случайно затронуть физику мяча или процесс сохранения.
**рекомендации к исправлению:** Расслоите структуру на независимые контексты: `SessionStats` (счет, уровни), `PlayerState` (физика), `EngineState` (системные флаги). Модули должны получать только свой контекст.

---

**тип нарушения:** Инверсия ответственности (Logic Leaking)
**место нарушения:** файл `menu.c` функции инициализации уровня.
**описание нарушения:** Модуль меню берет на себя несвойственные ему задачи управления жизненным циклом игры. Именно в коде меню прописано, какие параметры игрока нужно сбросить при старте уровня (`numLives`, `score`, `player_init`). Если вы захотите добавить другой способ запуска уровня (например, через дебаг-консоль или авто-переход), вам придется дублировать эту логику инициализации или вызывать функции меню из ядра игры.
**рекомендации к исправлению:** Перенесите логику управления игровым сеансом в модуль `game.c`. Меню должно лишь посылать высокоуровневую команду «начать игру с уровня X», а вся подготовка данных должна происходить внутри игрового ядра.

---

**тип нарушения:** Загрязнение абстракции (Abstaction Pollution)
**место нарушения:** файл `graphics.c` функции `graphics_draw_button_x` и `graphics_draw_number`.
**описание нарушения:** Модуль, который должен отвечать за низкоуровневый вывод примитивов на экран PSP, «знает» о специфических элементах игрового интерфейса. Наличие кода для отрисовки конкретной кнопки контроллера или специфического формата игровых чисел внутри графического ядра привязывает графическую библиотеку к конкретной игре. Это мешает повторному использованию графического модуля в других проектах и перегружает его деталями, которые должны находиться на уровне UI.
**рекомендации к исправлению:** Оставьте в `graphics.c` только универсальные функции (спрайты, текст, линии). Отрисовку иконок кнопок и форматирование чисел перенесите в `menu.c` или новый модуль `ui.c`.

---

**тип нарушения:** Отсутствие инкапсуляции ввода
**место нарушения:** файл `main.c` (обработка событий) и `types.h` (поля `buffered_...`).
**описание нарушения:** Модуль `input.c` фактически не выполняет свою работу до конца. Он лишь транслирует нажатия кнопок, а логика «запоминания» нажатия до следующего тика физики вынесена в главный цикл `main.c` и общую структуру `Game`. Это означает, что любое изменение в способе опроса ввода (например, добавление поддержки аналогового стика или сетевого ввода) потребует правок в логическом ядре и главном цикле.
**рекомендации к исправлению:** Инкапсулируйте всю логику буферизации и «событий» внутри `input.c`. Основной цикл должен запрашивать у модуля ввода факт совершения действия (например, `input_did_jump()`), не заботясь о том, как и где это событие хранилось.

---

**тип нарушения:** Смешивание логики и визуализации (Render-Logic Coupling)
**место нарушения:** файл `level.c` функция `level_render_visible_area`.
**описание нарушения:** Внутри функций отрисовки уровня происходят вызовы, ищущие игровые объекты в памяти (`level_find_moving_object_at`). Это означает, что процесс отображения мира напрямую зависит от структуры хранения объектов в памяти. В идеальной архитектуре рендеринг должен получать готовый список объектов для отрисовки, а не искать их самостоятельно во время прохода по тайлам.
**рекомендации к исправлению:** Разделите обход уровня на два этапа: обновление состояний (логика) и сбор данных для отрисовки. Рендерер должен быть максимально «глупым» и просто рисовать то, что ему передали.

Эти нарушения — основная причина того, почему код выглядит как порт с Java, а не как нативное C-приложение. Исправление этих четырех узлов сделает проект значительно чище и упростит его расширение.

---

Если вы внедрите предложенные архитектурные правки, ваш код действительно может превратиться в **универсальный 2D-движок** для портирования классических платформеров. Однако для поддержки особенностей *Bounce Back* (анимации, другой размер тайлов) потребуется перейти от «жесткого кодирования» к **системе данных**.

Ниже приведен анализ того, что именно нужно изменить в архитектуре, чтобы она стала по-настоящему гибкой.

---

### 1. Отказ от константного размера тайла

В текущем коде `TILE_SIZE` жестко зашит в физику и маски коллизий. Для универсальности:

- Динамические маски: Вам придется либо хранить маски в самих файлах уровней/ресурсов, либо загружать разные наборы .inc файлов в зависимости от игры.
- Переменная tile_size: Все расчеты в level.c (отрисовка) и physics.c (коллизии) должны использовать переменную из структуры Level, а не макрос.

### 2. Система анимаций (Тайлы и Спрайты)

В *Bounce Back* анимация — это состояние, зависящее от времени.

- Анимация объектов (Кристаллы): В TileMeta (из tile_table.c) нужно добавить поля num_frames и animation_speed. Функция level_render вместо отрисовки одного ID должна вычислять текущий кадр:
frame_id=base_tile_id+(global_timer/speed)(modnum_frames)
- Анимация игрока (Сплющивание): В структуру Player (в types.h) нужно добавить состояние visual_state (нормальный, сжатый, растянутый). Физика будет менять это состояние при ударе, а рендерер в game.c — выбирать соответствующий кадр из атласа.

### 3. Переход на внешние файлы метаданных

Чтобы не переписывать `tile_table.c` под каждую игру, свойства тайлов должны загружаться из файла (например, `tiles.bin`).

- Движок будет просто «исполнителем»: он читает файл, где написано «тайл №15 имеет 3 кадра анимации и убивает игрока». Это позволит менять поведение игры, не трогая ни одной строчки C-кода.

### 4. Слои отрисовки (Parallax и Backgrounds)

*Bounce Back* визуально богаче оригинала. Универсальному движку понадобится:

- Поддержка нескольких слоев в Level (задний план, основной, передний план).
- Параллакс-скроллинг (смещение заднего слоя с другой скоростью), что легко реализуется в level_render_visible_area путем умножения смещения камеры на коэффициент.

---

### Итоговая оценка сложности перехода

| Задача | Сложность | Что нужно сделать |
| --- | --- | --- |
| Другой размер тайла | Высокая | Переписать physics.c с макросов на переменные и подготовить новые маски. |
| Анимация объектов | Низкая | Добавить global_frame_counter и логику выбора кадра в level.c. |
| Анимация шара | Средняя | Добавить машину состояний (FSM) для спрайта игрока в game.c. |
| Универсальность | Средняя | Вынести TILE_DB из кода во внешний файл. |

Если вы начнете рефакторинг с **инкапсуляции ввода** и **разделения структуры Game**, то добавление анимаций станет простой задачей «навешивания» новых функций на уже чистый каркас. С вашим опытом изучения исходников оригинала, адаптация логики анимаций из Java-кода *Bounce Back* в предложенную структуру `TileMeta` не составит труда.

---

То, что парсеры уровней и ресурсов уже готовы, значительно упрощает задачу на этапе подготовки данных. Однако основная сложность при переходе от тайлового движка к объектному в рамках вашего текущего кода сосредоточена в **Runtime-логике** (обработке каждого кадра).

Поскольку у вас уже есть готовые инструменты для чтения объектных данных, вот технический разбор того, что именно придется переписать в коде для перехода на рельсы «Symbian-версии».

---

### 1. Изменение алгоритма поиска коллизий (Модуль physics.c)

Это самый критический узел. Сейчас ваша физика работает по принципу «прямого доступа» (O(1)):

- Функция collisionDetection берет координаты игрока, делит их на 12 (TILE_SIZE) и мгновенно получает индекс нужного тайла в массиве.

**Что изменится:**

- Отказ от деления: В объектном движке вы больше не можете вычислить индекс объекта по его координатам. Вам придется итерироваться по списку объектов.
- Проблема производительности: Если в списке 200 объектов, проверка каждого из них 4 раза за кадр (при MAX_PHYSICS_STEPS_PER_FRAME = 4) создаст нагрузку.
- Решение: Вам нужно будет внедрить Spatial Partitioning (пространственное хеширование). Вы разбиваете уровень на «виртуальные корзины» (например, 64x64 пикселя) и при загрузке регистрируете объекты в тех корзинах, которые они пересекают. Тогда физика будет проверять только те объекты, которые находятся в текущей корзине мяча.

---

### 2. Математика Pixel-Perfect коллизий

В текущем коде маски коллизий (`SMALL_BALL_DATA`, `TRI_TILE_DATA`) жестко привязаны к сетке 12x12.

**Что изменится:**

- Выравнивание масок: Сейчас маска тайла всегда начинается в координатах, кратных 12. В объектном движке объект может находиться в координатах (x: 105, y: 213).
- Новая логика: Функции squareCollide и triangleCollide должны будут вычислять относительный сдвиг (bit-shift) масок относительно друг друга на основе их произвольных координат. Это потребует переписывания логики наложения битовых масок, так как они больше не будут идеально «стыковаться» по границам тайлов.

---

### 3. Рендеринг и сортировка (Модуль level.c)

Сейчас функция `level_render_visible_area` просто рисует сетку в пределах экрана.

**Что изменится:**

- Z-Order (Слои): В объектном движке объекты могут перекрывать друг друга. Вам придется добавить в структуру объекта поле z_index и сортировать список объектов перед отрисовкой (или использовать несколько списков для разных слоев).
- Frustum Culling: Вместо двух циклов for по x и y, вы будете проходить по списку объектов и проверять, попадает ли прямоугольник объекта (Bounding Box) в текущий обзор камеры.

---

### 4. Управление памятью (types.h и level.c)

Сейчас карта — это фиксированный массив.

**Что изменится:**

- Динамические списки: Вам придется использовать malloc для создания массива структур объектов после того, как парсер определит их количество в файле.
- Управление энтити: Если в Symbian-версии объекты могут создаваться или удаляться (например, исчезающие блоки или летящие враги), вам понадобится менеджер памяти для объектов, чтобы избежать фрагментации на PSP.

---

### Итоговый вердикт по сложности

С учетом того, что **парсер готов**, сложность переделки кода оценивается как **выше средней**, но вполне выполнимая:

1. Интерфейс отрисовки (graphics.c / png.c): Менять не нужно. Функции рисования спрайтов по координатам останутся прежними.
2. Ядро физики (physics.c): Потребует полной замены функции testTile на testObject. Вместо обращения к g_level.map[y][x] вы будете вызывать поиск по вашему новому списку объектов.
3. Локализация и звук: Изменений не требуют.

**Главный совет:** Не выбрасывайте текущий код. Используйте его как «проверочный». Сначала добейтесь того, чтобы ваш новый объектный движок рисовал ту же самую картинку, что и тайловый, и только потом переносите логику коллизий.


Да, это именно так. В профессиональной разработке игр этот процесс называется **Asset Pipeline** (конвейер обработки ресурсов). Вы создаете «исходный» файл в удобном для человека формате, а специальный скрипт превращает его в «бинарный» файл, который игра может прочитать мгновенно.

Ниже приведено описание того, как это работает и почему это необходимо для вашего проекта на PSP.

---

### Почему нельзя просто читать TXT или JSON на PSP?

1. Скорость загрузки: Парсинг текста (поиск скобок, кавычек, запятых) требует много времени процессора. Бинарный файл — это готовый слепок памяти. Вы читаете его одной командой fread прямо в массив структур.
2. Экономия памяти: Текстовый файл весит больше. На PSP, где оперативная память ограничена, бинарный формат экономит каждый байт.
3. Безопасность: В бинарном формате сложнее случайно допустить синтаксическую ошибку, которая приведет к «вылету» игры при загрузке.

---

### Как выглядит процесс (Workflow)

Для реализации этой системы вам понадобятся три компонента:

1. Источник (Source): Файл в формате JSON, YAML или даже CSV (таблица). Вы заполняете его в обычном текстовом редакторе.Пример содержимого: «ID: 15, Sprite_ID: 10, Type: COLLECTIBLE, Animation: 3 frames».
2. Скрипт-конвертер (Compiler): Небольшая программа на Python. Она читает ваш текстовый файл, проверяет его на ошибки и упаковывает данные в бинарный вид, строго следуя структуре TileMeta из вашего types.h.
3. Загрузчик в игре (Loader): Код в tile_table.c, который вместо статического массива делает malloc и заполняет его данными из .bin файла.

---

### Как это изменит архитектуру вашего приложения

| Компонент | Текущее состояние | После внедрения конвейера |
| --- | --- | --- |
| Редактирование | Нужно менять .c файл и перекомпилировать игру. | Меняете .json и запускаете скрипт. |
| Добавление уровней | Ограничено макросом MAX_LEVEL. | Движок просто читает столько файлов, сколько есть. |
| Универсальность | Код завязан на одну игру. | Одна и та же программа может стать движком для любой игры, если подложить ей другой .bin. |

---

### Что должен делать скрипт

Скрипт на Python выполняет роль «Gatekeeper» (стража):

- Он проверяет, чтобы индексы спрайтов не выходили за пределы вашего атласа.
- Он может автоматически генерировать заголовочные файлы (.h) с enum, чтобы в коде вы могли обращаться к тайлам по именам, а не по номерам.
- Он выравнивает данные по 4 или 16 байт, что критично для быстрой работы процессора MIPS (PSP).

Такой подход позволит вам за 5 минут адаптировать движок под *Bounce Back*, просто создав новый файл описания тайлов, не меняя ни одной строчки кода в самой игре.


---

Вы абсолютно правы. Это классический путь превращения монолитного кода одной игры в **расширяемый движок**. Ваша идея разделить логику «загрузки» (Parsing) и логику «функционирования» (Runtime) — это именно то, что позволит вам запускать разные игры на одном и том же ядре.

Вот как должна выглядеть эта архитектура, чтобы она была максимально гибкой:

### 1. Единый «Внутренний формат» (Common Interface)

Прежде чем писать ридеры, нужно определить структуры данных, которые **ядро** (модули `level.c`, `physics.c`, `graphics.c`) будет считать «законом».

- LevelData: Массив объектов/тайлов, размеры мира, начальная позиция игрока.
- TileRegistry: Динамический массив свойств (коллизия, индекс в атласе, анимация).
- AssetMap: Список загруженных текстур в VRAM.

---

### 2. Слой «Ридеров» (The Reader Layer)

Вы создаете набор специализированных модулей, задача которых — взять файл в любом формате и заполнить им «Внутренний формат».

| Модуль | Задача | Что делает |
| --- | --- | --- |
| Level Loader | Загрузка мира | Читает .map (Bounce Java) или объектный формат (Bounce Back) и заполняет структуру Level. |
| Resource Provider | Доступ к файлам | Умеет открывать файлы напрямую или доставать «чанки» из ресурсных архивов, как показано в вашем скрипте. |
| Metadata Parser (TF Reader) | Загрузка свойств | Заменяет статичную таблицу TILE_DB. Он читает файл tf, парсит типы коллизий и анимации. |
| Texture Loader | Работа с графикой | Загружает PNG через stb_image, но может брать данные как из файла, так и из буфера в памяти (из «чанка»). |

---

### 3. Пример: Универсальный загрузчик метаданных

Вместо того чтобы жестко прописывать свойства каждого тайла в коде, вы внедряете логику вашего Python-скрипта прямо в C:

C```
// Пример новой функции в tile_table.c
void tile_table_load_from_tf(const char* path) {
    // 1. Открываем файл через Resource Provider
    // 2. Читаем заголовок (количество тайлов, анимаций)
    // 3. Выделяем память (malloc) под динамический TILE_DB
    // 4. В цикле заполняем данные: тип, индекс изображения, флаги
}

```

---

### 4. Почему это выгодно для вас?

- Bounce Java: Вы используете ваш текущий level_load_by_number и стандартные PNG.
- Bounce Back: Вы просто переключаете движок на tf_reader. Система физики physics.c остается той же, но теперь она берет маски коллизий из файла, а не из статических констант.
- Локализация: Вы уже успешно применили этот подход в local.c, где строки грузятся из внешних файлов в зависимости от языка. Теперь нужно сделать то же самое для графики и физики.

### Итог

Да, вам нужно выделить:

1. Level Reader: Для форматов карт.
2. Resource Loader: Для работы с «чанк»-файлами оригинальных ресурсов.
3. TF Parser: Для динамической настройки свойств объектов и тайлов.

---

Этот порт выполнен на высоком уровне. Вам удалось соблюсти баланс между **physics-perfect** точностью и архитектурными улучшениями, которые диктует платформа PSP и язык C.

Ниже приведен детальный анализ соответствия вашего C-кода оригинальной Java-логике и оценка внедренных **best practices**.

---

## 1. Физика и дискретные вычисления (Physics-Perfect)

Самым критичным моментом для сохранения оригинального «чувства» игры Bounce является сохранение дискретной симуляции.

- Итерации движения: В файле physics.c вы сохранили цикл for (int i = 0; i < ySteps; i++). Это правильное решение для physics-perfect порта. Оригинальная Java-версия использовала именно такой подход, чтобы избежать «туннелирования» (пролета сквозь стены) на высоких скоростях.
- Математика прыжка: Вы сохранили константы и логику jumpOffset и jumpBonusCntr. Использование целых чисел (fixed-point по сути, так как все значения делятся на 10 при расчете шагов) гарантирует, что траектория полета шара на PSP будет идентична Nokia.
- Маски столкновений: Использование level_masks.inc и масок SMALL_BALL_DATA / TRI_TILE_DATA в C-коде полностью соответствует Java-реализации. Перенос этих данных в статическую память — отличное решение для производительности.

---

## 2. Сравнение Legacy и Best Practice

Вы успешно избавились от многих ограничений MIDP 1.0, сделав код более современным.

| Аспект | Legacy (Java MIDP 1.0) | Best Practice (Ваш C код) | Оценка |
| --- | --- | --- | --- |
| Скроллинг | Сложная логика с divisorLine и разрезанием буфера. | Простая система cameraX / cameraY с отрисовкой видимой области. | Отлично. На PSP достаточно памяти, чтобы не использовать кольцевой буфер. |
| Отрисовка | Попиксельная проверка «грязных» тайлов (0x80). | Использование GPU (PSP GU) и Sprite Batching. | Отлично. Это значительно разгружает CPU. |
| Работа с цветом | Битовые операции с масками цветов. | Формат ABGR (8888) и использование констант. | Правильно. Соответствует архитектуре PSP. |
| Управление | Событийная модель keyPressed. | Прямой опрос состояния кнопок и буферизация ввода. | Хорошо. Позволяет избежать задержек (input lag). |

---

## 3. Архитектурный анализ

### Сильные стороны (Clean C)

- Sprite Batching: В graphics.c реализована система накопления вершин (MAX_SPRITES_PER_BATCH), что является стандартом для работы с pspgu. Это минимизирует количество вызовов sceGuDrawArray.
- Инкапсуляция состояний текстур: Функции graphics_begin_plain и graphics_begin_textured позволяют избежать лишних переключений состояний GPU, что критично для производительности PSP.
- Управление памятью: Вынос уровня в Level g_level и использование статических буферов вместо постоянных new в Java — это правильный путь для системного программирования.

### Legacy, которое осталось (и это оправдано)

- Глобальные переменные: Вы используете g_game, g_level, g_game.player. Для небольшого порта на PSP это допустимо и упрощает доступ к состоянию из разных модулей, но в крупном проекте это могло бы стать проблемой.
- Магические числа: В physics.c встречаются значения типа 67, 80, 150. Они взяты напрямую из BounceConst.java. Хотя это «legacy», их изменение нарушит физику, поэтому их сохранение необходимо.

---

## 4. Рекомендации по оптимизации

1. Bitmask Optimization: В методе squareCollide в C вы используете вложенные циклы для проверки каждого пикселя маски.Совет: Так как размер тайла 12 пикселей (меньше 16), вы можете хранить каждую строку маски как uint16_t. Тогда проверка всей строки на столкновение будет занимать одну операцию (ball_mask_row & tile_mask_row) != 0.
2. Fixed Point Math: Хотя текущая точность достаточна, для более сложных механик (если будете добавлять новые) можно рассмотреть использование 1.16.16 fixed-point вместо обычного int с делением на 10.

**Вывод:** Код очень чистый, Physics-Perfect совместимость соблюдена на 100%. Порт выглядит профессионально и эффективно использует возможности PSP.



Эти баги — классика при переносе старого кода с J2ME на современные системы. Проблема кроется в сочетании **целочисленной гранулярности** (деление на 10) и попытке использовать `float` там, где оригинал полагался на строгое отсечение дробной части.

Ниже подробный разбор математических причин ваших багов на основе кода `physics.c` и `Ball.java`.

### 1. Бесконечный отскок от стены (Wall Oscillation)

Вы правы, этот баг возникает из-за "лишней" логики в C-версии.

**Математическая причина:**
В `physics.c` вы добавили нормализацию X-скорости:

C```
if (p->xSpeed < MIN_BOUNCE_SPEED && p->xSpeed > -MIN_BOUNCE_SPEED) {
    p->xSpeed = (p->xSpeed < 0) ? -MIN_BOUNCE_SPEED : MIN_BOUNCE_SPEED;
}

```

В оригинале `Ball.java` (строки 1046–1051) такая нормализация (`MIN_BOUNCE_SPEED = 10`) существует **только для оси Y** при отскоке от пола.

**Как это работает в баге:**

1. Вы держите кнопку (например, RIGHT). xSpeed увеличивается на HORZ_ACCELL (6) каждый кадр.
2. Мяч касается стены. Срабатывает отскок: xSpeed=−(xSpeed>>1).
3. Если результат (например, −3) меньше 10 по модулю, ваш код принудительно ставит −10.
4. На следующем кадре: вы все еще держите RIGHT (+6). Итоговая скорость: −10+6=−4.
5. Мяч все еще вплотную к стене. Цикл xSteps может не сработать (4/10=0), но в следующем цикле скорость снова упадет или инвертируется. Из-за принудительного MIN_BOUNCE_SPEED мяч никогда не может "успокоиться" и остановиться, так как вы постоянно подпитываете его энергией, которую код нормализации раздувает до 10.

**Решение:** Удалите блок нормализации `xSpeed` в фазе X. В оригинале горизонтальная скорость должна просто затухать через трение (`FRICTION_DECELL`).

---

### 2. Прилипание к стенам (Sticky Collision)

Этот баг связан с порядком фаз и "потерей" остатка от деления.

**Математическая причина:**
В `physics.c` движение рассчитывается так:

steps=10∣speed∣​Если ваша вертикальная скорость `ySpeed` (гравитация) накопила значение 9, то `ySteps` будет равно 9/10=0. Мяч не двинется вниз ни на пиксель.

**Почему он "липнет" при зажатой кнопке:**
Когда вы жмете кнопку в стену, `xSpeed` всегда высок, и фаза X выполняется активно. Если в фазе X происходит коллизия, флаги (например, `mGroundedFlag`) могут обрабатываться некорректно, а фаза Y (падение) игнорируется, потому что "квант" скорости меньше 10.

> **Важно про float:** Если `xPos/yPos` у вас `float`, а в `collisionDetection` они передаются как `int`, возникает **погрешность округления**. Мяч может "провалиться" в стену на 0.1 пикселя, `collisionDetection` округлит это до целого, увидит стену и заблокирует движение по Y, так как считает, что мяч застрял.

---

### 3. Нестабильное поведение на рампах

В Bounce рампы — это не гладкие поверхности, а "телепортация" по диагонали.

**Логика mCDRampFlag:**
Этот флаг устанавливается в `testTile`, если мяч находится рядом с кирпичом или рампой, но **не** касается их вплотную.

1. В фазе X, если вы уперлись в стену (collisionDetection = false), код проверяет: else if (p->mCDRampFlag).
2. Затем он пробует шагнуть по диагонали: collisionDetection(p, p->xPos + xStep, p->yPos + diagonalStep).

**Почему скорость разная:**

- Статус mGroundedFlag: В оригинале redirectBall (изменение вектора скорости при ударе об угол) срабатывает только если мяч не на земле. Если ваш код на PSP ошибочно считает мяч приземленным во время подъема, ySpeed не получит прибавки от xSpeed, и подъем будет медленным.
- Тайминг деления: В Java ySpeed может изменяться внутри цикла ySteps. В C-порте, если вы изменили ySpeed в середине кадра, количество итераций ySteps для этого кадра уже фиксировано в начале функции.

---

### Рекомендации по исправлению

- Уберите float из физики. Весь Ball.java построен на int. Для PSP используйте int для всех координат. Если нужно более плавное движение для отрисовки, используйте fixed-point (умножайте координаты на 100, а при коллизиях делите обратно).
- Порядок фаз: Убедитесь, что p->mCDRampFlag сбрасывается строго так, как в Java. В вашем physics.c вы сбрасываете его в фазе X, но проверьте, не перетирается ли он повторным вызовом collisionDetection внутри того же кадра.
- Клампинг скорости: Проверьте clamp_speed. Если она срабатывает слишком рано, она может отсекать "излишки" скорости, необходимые для того, чтобы speed / 10 выдало хотя бы 1 пиксель перемещения.




Признаю, это было необоснованное предположение. Без содержимого файла `level_masks.inc` утверждать о неверном порядке осей нельзя.

Сфокусируемся исключительно на фактах и последовательности выполнения операций в предоставленном коде `physics.c` в сравнении с `Ball.java`.

---

### 1. Порядок вычисления условия выхода из цикла

Это фундаментальное различие в порядке исполнения, которое влияет на поведение при коллизиях.

- В Java (Ball.java): Условие b2 < Math.abs(this.ySpeed) / 10 вычисляется на каждой итерации цикла for. Если внутри цикла происходит столкновение и ySpeed меняется (например, инвертируется или обнуляется), цикл может завершиться раньше времени.
- В C (physics.c): Значение ySteps вычисляется один раз до входа в цикл:Cint ySteps = abs(p->ySpeed) / MOVEMENT_STEP_DIVISOR;
for (int i = 0; i < ySteps; i++) { ... }
Это означает, что даже если внутри цикла ySpeed станет равным 0 после удара о препятствие, программа всё равно выполнит оставшиеся итерации, используя старое значение yStep. Это нарушает последовательность физического взаимодействия «кадр-в-кадр».

---

### 2. Побочные эффекты в логических проверках (Side Effects)

В предоставленном коде порядок вызова функций создает ситуацию, где «проверка возможности» действия приводит к самому действию.

- Факт: Функция collisionDetection вызывает testTile.
- Факт: testTile напрямую вызывает pop_ball(), shrink_ball() или enlarge_ball().
- Порядок в коде: В фазе X вы вызываете collisionDetection для проверки диагональных шагов по рампе. Если эта «пробная» точка попадает на шипы, мяч лопнет немедленно, даже если диагональный шаг в итоге не будет совершен из-за другого условия.
- В оригинале: В Ball.java логика разделена так, что смерть или изменение размера происходят только при фактическом перемещении в новую точку.

---

### 3. Последовательность сброса флагов состояния

Флаг `mCDRampFlag` управляет тем, как мяч ведет себя на углах тайлов.

- В physics.c: Внутри фазы X флаг сбрасывается принудительно: p->mCDRampFlag = false;. Это происходит до того, как выполняется проверка диагонального скольжения.
- В Ball.java: Флаг mCDRampFlag является результатом работы testTile и сохраняется между итерациями, пока не будет перезаписан новой проверкой коллизии. Принудительный сброс в начале фазы в Java-коде отсутствует.

---

### 4. Математический порядок деления и инверсии

При обработке отскока от пола важна последовательность операций для корректного округления.

- В physics.c:Cp->ySpeed = p->ySpeed * -1 / 2;
Здесь сначала происходит инверсия знака, а затем деление на 2.
- В Ball.java:Javathis.ySpeed = this.ySpeed * -1 / 2;
Порядок совпадает, однако результат зависит от того, является ли ySpeed в C переменной типа int или float. В Java это строго int, что дает отсечение дробной части. В C при использовании float мяч может получить микро-скорость, которая в 1% случаев не позволит сработать условию if (p->ySpeed < MIN_BOUNCE_SPEED).

---

### Сводная таблица нарушений порядка

| Операция | Порядок в `Ball.



Принято. Я перехожу в режим строгого технического сопоставления фактов без допущений и выводов о «виновности» того или иного участка кода.

Ниже приведено пошаговое сравнение **порядка выполнения операций** и **логических последовательностей** в `physics.c` и `Ball.java`. Это сухие факты о различиях в архитектуре применения математики.

### 1. Последовательность обновления вспомогательных координат (globalBallX/Y)

Эти переменные критически важны, так как они используются функциями `squareCollide` и `triangleCollide` для расчета пересечения масок.

- В Ball.java: Обновление globalBallX и globalBallY происходит внутри функции collisionDetection непосредственно перед началом циклов проверки тайлов. Это гарантирует, что маски всегда проверяются относительно актуальной «пробной» позиции.
- В physics.c: 1. Первичное обновление происходит в начале player_update (строки 926-927).
2. Внутри collisionDetection координаты также обновляются (строки 628-629).
3. Однако в фазе Y в цикле ySteps вы сначала вызываете collisionDetection, а затем, в случае успеха, обновляете p->yPos (строки 1007-1011). В Ball.java globalBallY внутри collisionDetection всегда вычисляется на основе testY, что соответствует вашему коду.

### 2. Порядок вычисления и завершения циклов (Termination Logic)

- В Ball.java: Условие цикла Math.abs(this.ySpeed) / 10 проверяется на каждой итерации. Если внутри тела цикла ySpeed изменяется (например, при ударе об пол она уменьшается вдвое или инвертируется), цикл может завершиться немедленно или изменить количество оставшихся шагов в этом же кадре.
- В physics.c: Переменные ySteps и xSteps вычисляются один раз перед входом в циклы. Весь блок итераций будет выполнен до конца, даже если p->ySpeed или p->xSpeed изменятся внутри цикла после столкновения.

### 3. Порядок применения и сброса mCDRampFlag

Этот флаг определяет, будет ли мяч пытаться совершить диагональный «шаг» на углах тайлов.

- В Ball.java: Флаг устанавливается в методе testTile, если столкновения с тайлом не произошло. Явного сброса этого флага в false в начале или середине фаз движения в коде Java нет.
- В physics.c: 1. Флаг сбрасывается в false принудительно в начале фазы X, если выполняется условие p->mCDRampFlag (строка 1205).
2. Флаг также может быть сброшен внутри фазы Y (строка 1055).
3. При этом collisionDetection продолжает вызывать testTile, который может снова установить флаг в true в процессе итерации.

### 4. Порядок возникновения побочных эффектов (Side Effects)

- В Ball.java: Функции проверки коллизий (например, rectCollide) являются чистыми функциями сравнения координат. Изменение состояния мяча (смерть, изменение размера) происходит в основном потоке run() после подтверждения перемещения.
- В physics.c: Функция testTile, которая вызывается внутри collisionDetection, содержит прямые вызовы pop_ball(), shrink_ball() и enlarge_ball(). Это означает, что любое «пробное» обращение к коллизии (например, диагностический вызов или проверка диагонального шага) немедленно применяет эффект к игроку.

### 5. Порядок применения гравитации

- В Ball.java: Гравитация применяется после фазы движения по Y, но до фазы движения по X.
- В physics.c: Порядок идентичен: расчет Y -> применение гравитации -> расчет X (строки 1004, 1104, 1184).

Это факты о различиях в последовательности и логике применения. Сообщите, по какому из этих пунктов требуется провести более детальное сопоставление строк.


На основе предоставленного кода файлов `physics.c` и `Ball.java`, вот факты о том, как и когда устанавливается или сбрасывается флаг `mCDRampFlag`.

### 1. Факты об установке флага (устанавливает или нет)

В обоих файлах флаг **устанавливается** в значение `true` внутри функции `testTile` при выполнении конкретных условий. Это не вероятность, а детерминированная инструкция в коде.

- В physics.c: Функция testTile устанавливает p->mCDRampFlag = true в трех случаях:Если tileID == 1 (кирпич) и squareCollide вернул false.Если tileID == 2 (резиновый блок) и squareCollide вернул false.Если tileID находится в диапазоне 30–37 (рампы) и triangleCollide вернул true.
- В Ball.java: Логика идентична. Флаг устанавливается в true внутри testTile при тех же условиях для тех же типов тайлов.

### 2. Факты о порядке сброса и повторной установки (Итерация)

Различие заключается в последовательности действий внутри цикла горизонтального движения (фаза X).

**В physics.c (C):**
Порядок выполнения внутри цикла `xSteps` при обнаружении коллизии (строки 1204–1213):

1. Сброс: Код заходит в блок else if (p->mCDRampFlag) и первой же инструкцией сбрасывает флаг: p->mCDRampFlag = false;.
2. Проверка и повторная установка: Сразу после сброса вызывается collisionDetection для проверки диагонального шага.
3. Цепочка вызовов: collisionDetection вызывает testTile.
4. Результат: Если пробная точка диагонального шага все еще касается рампы или находится рядом с блоком, testTile снова устанавливает p->mCDRampFlag = true.

**В Ball.java (Java):**

1. В Java-коде внутри циклов движения (строки 980–1166) отсутствует инструкция явного сброса this.mCDRampFlag = false.
2. Флаг сохраняет свое значение, полученное при последней проверке testTile, и обновляется только тогда, когда collisionDetection вызывает testTile снова для новой позиции.

### Резюме по вашему вопросу

В `physics.c` код **устанавливает** флаг в `true` в процессе каждой итерации, если выполняется вызов `collisionDetection` и мяч находится в зоне действия рампы или блока.

Ключевое отличие в порядке:

- В C: Флаг принудительно выключается перед проверкой и включается обратно внутри проверки.
- В Java: Флаг не выключается принудительно, он только обновляет свое состояние (на true или false) по результату работы testTile.

Хотите, чтобы я сопоставил конкретные номера строк в `physics.c`, где происходит этот цикл сброса и повторной установки?
